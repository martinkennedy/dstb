## This is MK tutorial on Github

* This is an ordered list
* Second item in ordered list...

* OK, so now I'm editing in Github 
* Then, I'll pull this back to...
* local respository via 'git pull'...

* now I'm making changes in rstudio
* I'll save locally and then push

* Save (text). Add (git add (filename))
* git commit.  git push
* git pull... !!!!!
